{
    "name": "facilitator",
    "private": true,
    "scripts": {
        "dev": "echo 'dev'",
        "build": "echo 'build'",
        "lint": "eslint ."
    },
    "devDependencies": {
        "@lens-llama/config": "workspace:*",
        "typescript": "^5.3.3"
    }
}